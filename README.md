# bbcfood-etl

Created a data pipeline using Docker which scrapes the BBC Food recipe catalogue, stores the data in a Postgres database. A Flask API was also created for fetching the data from the Postgres database
